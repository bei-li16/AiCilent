# 开发计划

## 待办

- [ ] **1. exe 集成启动器**
  - Windows 下双击即可启动代理服务，无需手动开终端敲命令
  - 可选：托盘图标，右键菜单启动/停止/打开监控面板
  - 集成 help 说明与排版优化，关键操作悬停显示简要提示

  **UI 排版布局：**

  原则：启动器只做**控制**（启停/状态/快速设置），监控交给 Web 面板，不重复堆数据。

  ```
  ┌───────────────────────────────────────────────┐
  │  AI Proxy Launcher  v1.0.16            ─ □ × │
  │───────────────────────────────────────────────│
  │                                               │
  │   ● 运行中           监听 :8080              │
  │   已运行 2h 13m      18 个供应商 · 3 优先级   │
  │                                               │
  │  ┌─────────────────────────────────────────┐ │
  │  │  ▶ 启动    ⏹ 停止    ↻ 重启              │ │  ← 悬停显示说明
  │  │  📊 打开监控面板 ↗    ⚙ 编辑配置         │ │
  │  └─────────────────────────────────────────┘ │
  │───────────────────────────────────────────────│
  │  快速设置                                     │
  │  ┌─────────────────────────────────────────┐ │
  │  │  监听地址    [:8080          ]           │ │  ← 悬停: 代理监听的地址端口
  │  │  日志级别    [ snippet    ▼  ]           │ │  ← 悬停: off/snippet/full 区别
  │  │  远程控制    [☑ 允许             ]       │ │  ← 悬停: /api/control 访问范围
  │  │  熔断阈值    [ 3 ]  冷却 [10]s  探测 [ 1] │ │  ← 悬停: 连续失败触发熔断
  │  │  默认路由    [ deepseek-v4-flash  ▼ ]     │ │  ← 悬停: 未匹配模型名时的兜底
  │  │                        [ 保存并重启 ]     │ │
  │  └─────────────────────────────────────────┘ │
  │───────────────────────────────────────────────│
  │  最近日志                          [ 清空 ]   │
  │  ┌─────────────────────────────────────────┐ │
  │  │ 15:30:12  服务启动完成，监听 :8080       │ │
  │  │ 15:30:12  加载 18 个供应商，3 个优先级组  │ │
  │  │ 15:31:05  gpt-4o → glm-5.2 (P1) ✓ 120ms │ │
  │  │ 15:31:08  default → deepseek (P2) ✓ 85ms│ │
  │  │                                (最近 5 条)│ │
  │  └─────────────────────────────────────────┘ │
  │───────────────────────────────────────────────│
  │  💡 悬停任意按钮/输入框可查看帮助说明          │
  └───────────────────────────────────────────────┘
  ```

  **托盘右键菜单：**
  ```
  ┌─────────────────────┐
  │ ▶ 启动服务          │
  │ ⏹ 停止服务          │
  │ ↻ 重启服务          │
  │─────────────────────│
  │ 📊 打开监控面板     │
  │ ⚙ 编辑配置          │
  │─────────────────────│
  │ 🖥 显示窗口          │
  │ ✕ 退出              │
  └─────────────────────┘
  ```

  **设计要点：**
  - 窗口固定窄宽（~480px），纵向排列，不占屏幕空间
  - 状态栏仅一行：运行指示灯 + 监听地址 + 运行时长 + 供应商数
  - 控制按钮区：4 个核心操作，悬停显示说明
  - 快速设置：常用全局参数就地修改，保存后自动重启生效
  - 日志区仅最近 5 条，完整日志点「打开监控面板」查看
  - 底部 help 提示行引导用户使用悬停功能

- [ ] **2. 手动配置供应商，自动生成配置 yaml**
  - 提供交互式引导（命令行或 Web 界面），逐项填写供应商信息
  - 根据输入自动生成 `providers.yaml`，降低手写 YAML 出错率
  - 支持追加/编辑已有供应商，不破坏现有配置

- [ ] **3. 控制台两栏信息支持 clear**
  - 监控面板的实时日志栏和统计栏增加「清空」按钮
  - 前端清空显示，不影响后端日志文件和统计数据

- [ ] **4. 模型名称对应的请求规则**
  - 不同模型名映射到不同的默认请求参数（temperature、max_tokens、timeout 等）
  - 在 `model_routes` 或新增配置段中按模型名定义规则
  - 客户端发送模型名后自动应用对应规则，无需手动指定

- [ ] **5. 请求与回复的唯一标识，实现真正并发追踪**
  - 为每个请求生成唯一 ID（如 UUID），贯穿请求→重试→上游响应全链路
  - 日志、统计、监控面板均可按请求 ID 关联查看
  - 评估现有代码是否已有 trace ID 机制，如有则完善，无则新增

- [ ] **6. 编译两种产物：gui（集成启动器）和 cli（纯命令行）**
  - `make build-gui` 产出 `ai-proxy-gui.exe`：内嵌启动器 + 托盘 + 快速设置（#1 的全部 UI）
  - `make build-cli` 产出 `ai-proxy.exe`：纯命令行，零 GUI 依赖（即现有模式）
  - **两种产物底层转发逻辑完全一致**，共享 `internal/` 全部包（router/engine、adapter、retry、tracer 等），仅入口层不同
  - 通过 build tag 分离 GUI 入口代码（`cmd/launcher/`），CLI 入口（`cmd/proxy/`）不受影响
  - GUI 模式启动后自动弹出启动器窗口，托盘可呼出/隐藏
